# Build output
.derivedData/
.build/
DerivedData/
.cache/

# Generated by XcodeGen from project.yml — do not commit
SwiftMaestro.xcodeproj/

# Excalidraw upstream SOURCE repo — kept locally only to rebuild the 23MB
# web bundle in Sources/Resources/excalidraw (the self-contained runtime asset
# that IS shipped/committed). The 1.1GB source tree (node_modules + .git) must
# never be committed or pushed to the public repo.
Vendor/excalidraw/

# Local tool config
.qwen/

# macOS
.DS_Store
build/

# Artifacts
*.dmg
*.dmg.sparse.sparseimage
*.sparseimage
*.sparsebundle

# Staging / distribution directories
dist/
BundledMCP/
store/

# Sparkle archive cache for delta update generation
.sparkle-archive-cache/

# Node (docs/mcp-template example server)
node_modules/
SwiftMaestro-0.1.0-full.dmg
SwiftMaestro-0.1.0-beta.dmg
**/.swiftpm/
Vendor/**/*.xcodeproj
Vendor/**/*.xcworkspace

# Firecrawl research cache
.firecrawl/

# Private: investor/outreach research must NEVER live in this repo (moved to ~/Documents/SwiftMaestro Notes)
docs/outreach/
outreach/

logs/
